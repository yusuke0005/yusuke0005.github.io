# CLAUDE.md

このリポジトリで作業するときに最初に読む。

---

## 0. このリポジトリ

GitHub Pages。公開先は `https://yusuke0005.github.io/`。

| パス | 中身 |
|---|---|
| `log/index.html` | **思考ログアプリ。単一HTML 1枚。ここが主戦場。** |
| `index.html` | トップページ |
| `note/` | 旧ノートアプリ。捨てる予定。**触らない。** |
| `todo_log.md` | メモ |

**公開リポジトリ。全世界から読める。** APIキー・GitHubトークン・個人情報を、コードにもコメントにもドキュメントにも書かない。

---

## 1. 絶対の原則

1. **単一ファイル。** `log/index.html` の中に HTML・CSS・JS を全部入れる。別ファイルに出さない。外部CDNを読み込まない。ビルド工程を入れない。
2. **全面書き直し禁止。** 必ず部分編集する。丸ごと書き直すと、コメントに書かれていない細かい挙動が毎回落ちる。
3. **意味を持たせない。** 色・枠線・タグ・やることに、既定の意味やラベルをコードへ埋め込まない。道具だけ作る。名前を付けるのは使う本人。「赤＝重要」のような対応をハードコードした時点で、その意味は動かせなくなる。
4. **項目を全部並べない。** 種類だけ出して、押したら中身が開く。選択バーも設定も同じ。一画面に全選択肢を敷き詰めない。
5. **頼まれていない機能を足さない。** 説明文・ヘルプ・ツールチップをUIに勝手に増やさない。
6. **原文（`raw`）はプログラムから絶対に書き換えない。** 整形・要約・トリムもしない。加工していいのは `org`（整理）だけ。
7. UI文言は日本語。短く、断定形。

---

## 2. 二度とやらないこと（過去に実際に壊した）

1. **`onclick="f('...')"` の中に `esc()` を使う。**
   アポストロフィが素通りして属性が閉じ、画面全体が死ぬ。
   - `esc()` … `& < >` だけ。テキストノード用。
   - `esa()` … `value="..."` などの属性用。
   - `jsa()` … **`onclick` の中のJS文字列リテラル用。属性の中に文字列を埋めるときは必ずこれ。**

2. **モバイルで固まる／落ちるCSS・JSを使う。**
   禁止：`position:fixed` / `position:sticky` / `<canvas>` / `animation ... infinite` / `backdrop-filter` / `filter:drop-shadow()` / 大きな blur 影 / `requestAnimationFrame` のループ / 外部CDN。
   可：`position:relative` + `absolute`、`transition`、`overscroll-behavior`。
   固定表示は flex で作る（左レール＋右本体）。

3. **`alert` / `confirm` / `prompt` を使う。** 一切使わない。確認も入力も自前のポップアップでやる。

4. **flex の子に `min-height:0` を付け忘れる。** Android で中身がはみ出し、下の入力欄が画面外へ落ちる。`.main` `.feed` など縦に伸びる箱には必ず付ける。

5. **`.feed` の `overscroll-behavior-x:contain` と `touch-action:pan-x pan-y` を外す。**
   横スワイプがブラウザの「戻る」に取られて、書きかけが飛ぶ。子要素（`#zoom` `.sheet` `.rows` `.row` `.row>div`）にも同じ `touch-action` が要る。Android は親だけだと効かない。

6. **通し番号 `n` を詰め直す。** 一度振った番号は二度と変えない。消しても欠番のまま空ける。詰めると、過去のまとめに書いた「#12」が別のブロックを指す。

7. **AIモデル名を世代番号で固定する。** その世代が新規に閉じられた日に 404 で全部止まる（Gemini 2.5 で実際に起きた）。Google側は `-latest` を使う。

8. **base64 を素朴に作る。** `btoa(unescape(...))` は UTF-8 で壊れ、`String.fromCharCode.apply` の一括呼び出しは大きいデータでスタックが溢れる。**TextEncoder → `0x8000` ずつ → `btoa`** の形を崩さない。

9. **キーやトークンをファイルに書く。** 公開リポジトリ。キーはユーザーが設定欄に貼り、`localStorage` にだけ入る設計を維持する。

10. **関数を消す前に呼び出し元を grep しない。** 消す前に必ず全文検索する。

11. **`BUILD` を上げ忘れる。** 変更したら `var BUILD='vNN';` を必ず上げる。ユーザーはこれで反映を確認する。

---

## 3. 触ると連鎖して壊れる組み合わせ

### A. 列レイアウト（いちばん壊れる）

```
.sheet / .head          width:62em
#zoom / #zhead          padding:0 1.2em
Zfit() の除数           64.4      ← 62 + 1.2×2
Zorg() の除数           43.4
.head                   font-size:.68em
.sep                    font-size:.7em
列幅  .c-date 5em / .c-title 12em / .c-org 24em / .c-raw 21em
```

**列を足す・減らす・幅を変えるときは、この全部を同時に直す。** どれか1つだけ変えると、拡大縮小したときに見出しと本体がズレる。

### B. 保存の連鎖

```
値を書き換える
  → persist()      … 中で numAll() を先に走らせて IndexedDB へ（entries / del / conf）
  → pushSoon()     … 1200ms デバウンスで sync()
```

**片方だけ呼ぶと端末間でズレる。** ゴミ箱・色・タグ・やること、どれを触っても両方呼ぶ。

### C. 同期と `data.json`

- 形は `{entries:[], del:[], conf:{}}` の3つだけ。増やすなら `mix()` と `sync()` の両方を直す。
- `mix()` は id 一致で `u`（最終更新）が新しい方を採る。だから**更新したら必ず `e.u=Date.now()` を入れる。** 忘れると他端末の古い版に負ける。
- 消したIDは `DEL` に積む。積まないと他端末から復活してくる。
- `CONF` は `u` を見て**丸ごと**新しい方に入れ替える。フィールド単位で混ぜない。混ぜると色定義とエントリの `c` が食い違って、無い色を指す行ができる。
- 409（他端末と衝突）は **1回だけ**自動リトライ。回数を増やさない。

### D. 描き直しの連鎖

エントリを触ったら、必要なものを呼ぶ：
`persist()` / `draw()` / `tags()` / `colbar()` / `bdbar()` / `trCnt()` / `pushSoon()`

`CONF.cols` `CONF.bds` `CONF.acts` を触ったら：
`colCss()` / `edDraw()` / `selbarDraw()`

`CONF.doneK` は「やることが全部済んだら自動で付く色」。`acts` から色を消すときは `doneK` も見る。

### E. 選択モードと日付タップ

`SELMODE`（選択モードON/OFF）・`SEL`（選ばれたID）・`SBP`（開いているポップアップ）の3つが噛み合っている。

`setIm()` 冒頭の `if(SELMODE) return;` は**意図的**。選択中の日付タップは「行を選ぶ」に取られるのが正しい。ここを外すと選択が効かなくなる。選択中に重要を付けるのは帯の「重要」ボタン。

選択モードを出入りしたら `SBP=''` してポップアップを閉じる。

### F. `all()` と `SEED`

`all()` は `MEM.concat(SEED)` を返す。`SEED` は id `s1` のサンプル1件。
`numAll()` は `MEM` だけを走る。**SEED に通し番号を振らない。**
一覧を回すコードで、`all()` と `MEM` のどちらが正しいかを毎回確認する。

---

## 4. データの形

### エントリ

| キー | 中身 |
|---|---|
| `id` | 一意。先頭1文字＋13桁ミリ秒（`msOf()` がここから時刻を取る） |
| `n` | 通し番号。一度振ったら変えない |
| `d` | 日付 `YYYY-MM-DD` |
| `tm` | 時刻 `HH:MM` |
| `t` | タイトル |
| `org` | 整理（加工してよい唯一の欄） |
| `raw` | 原文（**絶対に加工しない**） |
| `tag[]` | 分野タグ |
| `at[]` | やること（行動タグ） |
| `ax[]` | 消し込み済みのやること |
| `c` | 色キー（`CONF.cols[].k`） |
| `bd` | 枠線キー（`CONF.bds[].k`） |
| `im` | 1 なら重要（日付欄が赤） |
| `s` | 星 1〜5 |
| `z` | 1 ならゴミ箱 |
| `u` | 最終更新 ms。**更新時は必ず入れる** |
| `k` | `'sep'` なら区切り線 |
| `sp` `after` `before` | 分割の記録 |

### CONF

```js
var CONF={
  u:0,                      /* 最終更新 ms */
  cols:[{k,n,h,dim?}],      /* 色。ブロック全体の背景 */
  bds:[{k,n,h}],            /* 枠線（outline）。色と重ねられる */
  acts:[String],            /* やることの候補 */
  doneK:''                  /* やることが全部済んだとき自動で付ける色のキー */
};
```

### 保存の3層

1. 設定（キー・モデル・表示） … `localStorage`（`KEY` にキー名がまとまっている）
2. 端末内キャッシュ … IndexedDB（DB名 `log` / ストア `kv` / キー `entries` `del` `conf`）
3. 本体 … **別の**プライベートリポジトリの `data.json`（GitHub Contents API）

**本体はこのリポジトリには入らない。** アプリのコードと本人のログは別の置き場所。

---

## 5. AI呼び出し

- モデルは2本持つ。ふだん用とまとめ用。Anthropic / Google のどちらでも動く。
- `isAnth(key)` … キーが `sk-ant` で始まるかで判別。Anthropic は `max_tokens` 必須。
- 混雑（500 / 502 / 503 / 504）は `BUSY()` で判定し、`WAITS=[3,8,20]` 秒で自動再送。**黙って失敗させない。** 状態は必ず画面に出す。
- ストリーミング。落ちたら理由を画面に出す。

---

## 6. 検証のやり方

ブラウザ自動テストは使えない前提で組む（環境によって外部ダウンロードが通らない）。最低限これをやる：

```bash
cd log
sed -n '/^<script>$/,/^<\/script>$/p' index.html | sed '1d;$d' > /tmp/chk.js
node --check /tmp/chk.js && echo JS_OK
```

`node --check <(...)` はプロセス置換が使えず落ちる。**必ず一度ファイルへ書き出す。**

構文が通っただけでは足りない。変更した箇所の周辺を目で読み直す。特に §3 の連鎖に触れていないかを確認する。

---

## 7. 進め方

- 大きく作り替える前に、案を出して確認をとる。ただし**仕様が具体的に指示されたときは、それがGO。** 聞き返さずに作る。
- 変更は最小限。ついでの整理・リネーム・リファクタを混ぜない。
- 作業のたびに `BUILD` を上げ、コミットメッセージに何を変えたかを1行で書く。
- 反映は GitHub Pages。push から数十秒。キャッシュが強いので、確認するときは再読み込みする。
